const createAppHelper = ({ environment, route, components }) => {
    let script = `md react-app;cd react-app;md src;cd src;md components;cd components;`;
    console.log(script);
    
};

export default createAppHelper;